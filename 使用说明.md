# 刷题记录本 - 本地使用说明

## 启动

双击运行 **启动刷题记录本.bat**，浏览器会自动打开。

- 本机地址：http://127.0.0.1:8765
- 同局域网地址（手机/其他电脑）：
  - http://你的电脑名:8765
  - 或 http://你的电脑局域网IP:8765

## 创建桌面快捷方式

右键 **创建桌面快捷方式.ps1**，选择「使用 PowerShell 运行」。

## 数据

所有记录保存在当前文件夹的 **data.json** 中，建议定期备份。

- 每次保存前，程序会自动把上一版数据另存为 **data.json.bak**；万一写坏或误删，把它改名回 data.json 即可找回。
- 程序的运行日志写在 **server.log**（记录每次保存/删除请求），出问题时先看这个文件。

## 保存失败怎么办

- 出现红色提示「保存失败」时，说明这次的记录**没有写进 data.json**：内容已暂存在浏览器里，页面顶部会出现橙色提示条，等程序恢复正常后点「重新保存」就能补存。
- 保存成功后提示是绿色的「添加成功 ✓」；没看到这个提示就说明没存上。
- 请确认浏览器地址是 http://127.0.0.1:8765（关掉旧标签页重新打开），不要直接双击 web/index.html。

## 常见问题

- 如果 8765 端口被占用，程序会给出提示并自动改用 8766-8775 里的空闲端口（看命令行窗口里的实际地址）。
- 同局域网访问需要手机和电脑连接同一个 Wi-Fi。
- 请不要直接双击打开 web/index.html，那样无法保存数据。
